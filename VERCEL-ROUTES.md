# CareerGyan Vercel routes

The landing page is available at:
- /fair
- /event

Both routes rewrite to /index.html without changing the browser URL.
The routes are configured with revalidation-friendly cache headers.

Example:
https://events.careergyan.co/fair
